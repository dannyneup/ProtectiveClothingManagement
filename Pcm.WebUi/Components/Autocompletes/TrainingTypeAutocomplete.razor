<MudAutocomplete T="string" Value="TrainingType" ValueChanged="OnValueChanged"
                 Label="@Localization.trainingType"
                 SearchFunc="@SearchAutocomplete" ResetValueOnEmptyText="true"
                 Required="Required">
    <ItemTemplate>
        <div class="itemTemplate">
            <div class="itemContainerText">
                <MudText Typo="Typo.body1">@context</MudText>
            </div>
        </div>
    </ItemTemplate>
</MudAutocomplete>